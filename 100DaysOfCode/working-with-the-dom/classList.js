// remember this $0 only works in the dev tool
const heading = $0;
// checking if an element has a particular class
heading.classList.contains('text-center');
//returns true
// adding your own class
heading.classList.add('ice-cream');
 // buh we dont really need the class so lets remove it
 heading.classList.remove('ice-cream');
// then last buh definitely not the list is the toggle method
heading_one.classList.toggle('text-center');
//removes it from the page
// and toggling it again adds it back
heading_one.classList.toggle('text-center');
